#pragma once

struct Camera2S{
  // in meters
  int fovWidth;
  // in meters
  int fovHeight;
  Camera2S(int fovWidth = 0, int fovHeight = 0){
    this->fovWidth = fovWidth;
    this->fovWidth = fovWidth;
  }
};
